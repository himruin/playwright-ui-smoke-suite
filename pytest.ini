[pytest]
base_url = https://demo.playwright.dev/todomvc
addopts = --browser chromium --browser firefox
markers =
    smoke: baseline healthchecks, always runs in CI
    extended: extended Playwright techniques, run separately
